/* 
TODO add test driver for IR compatibility
1. Compile the IR from the rego code
2. Load the target IR from file
3. Compare

Note: comparison needs to compare by value, but everything is
referenced with integers. For strings, this is fine, but for
a lot of things this may be ambiguous.
*/